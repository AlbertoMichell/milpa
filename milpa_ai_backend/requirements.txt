# milpa_ai_backend/requirements.txt
# ============================================================
# Backend IA - Compatible con Python 3.11 a 3.14
# Usa versiones minimas (>=) para compatibilidad con wheels
# ============================================================

# Web & metricas
fastapi>=0.112.0
uvicorn[standard]>=0.30.3
prometheus-fastapi-instrumentator>=6.1.0

# Configuracion
pydantic>=2.8.2
pydantic-settings>=2.4.0

# Subida de archivos
python-multipart>=0.0.9

# Migraciones SQLite
yoyo-migrations>=8.2.0

# Antivirus (opcional, AV_OPTIONAL=true)
clamd>=1.0.2

# Utilidades generales
unidecode>=1.3.8
# Detección de idioma para OCR multilingüe (autodetección Tesseract)
langdetect>=1.0.9

# ============================================================
# Extraccion PDF/OCR/Tablas
# ============================================================
pymupdf>=1.25.0
python-docx>=1.1.2
pytesseract>=0.3.13
Pillow>=10.4.0
camelot-py>=0.11.0
pandas>=2.2.2
numpy>=1.26.4
regex>=2024.7.24
pint>=0.24.4
transformers>=4.44.2
sentencepiece>=0.2.0
# OpenCV para OCR por regiones (detección de columnas en escaneos)
opencv-python-headless>=4.8.0

# PDFs sintéticos para pruebas de estrés (opcional fuera de dev/CI):
reportlab>=4.0.0
matplotlib>=3.8.0

# NLP
spacy>=3.8.0

# ============================================================
# Embeddings, BM25, Vector DB
# ============================================================
sentence-transformers>=2.7.0
whoosh>=2.7.4
chromadb>=0.5.0
# tantivy 0.26+ expone TextAnalyzerBuilder + Filter (lowercase/ascii_fold/stemmer)
# que el BM25Index registra como analyzer "ml_es" (multilingüe español).
tantivy>=0.26.0

# ============================================================
# Tests
# ============================================================
pytest>=8.3.0
pytest-asyncio>=0.24.0

# ============================================================
# Contract testing / fuzzing (opcional para desarrollo)
# ============================================================
jsonschema>=4.23.0
schemathesis>=3.34.0
hypothesis>=6.100.0

# ============================================================
# OpenTelemetry (opcional, para trazas distribuidas)
# ============================================================
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-semantic-conventions>=0.48b0
opentelemetry-instrumentation>=0.48b0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
